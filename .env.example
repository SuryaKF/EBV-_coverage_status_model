# Postgres connection for ETL, training, and API service
DATABASE_URL=postgresql://username:password@hostname:5432/coverage_db

# Artifact root (file:// for local, s3:// bucket path for cloud)
ARTIFACT_ROOT_URI=file://./artifacts

# Feature version tagged in dataset/model registry
FEATURE_VERSION=v1

# API key required by /predict endpoint
API_KEY=change-me
